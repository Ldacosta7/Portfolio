// Utilisation de JavaScript pour activer l'animation
window.addEventListener('load', function () {
    var textElement = document.querySelector('.animate-text');
    textElement.style.opacity = '1'; // Rend le texte visible
});